<% content_for :title, "World Clock | Timezone Scheduler" %>

<div class="min-h-screen bg-slate-50 text-slate-900" data-controller="world-clock">
  <header class="border-b border-slate-200 bg-slate-50">
    <div class="mx-auto flex h-12 max-w-3xl items-center px-4 sm:px-6">
      <h1 class="text-base font-bold tracking-tight sm:text-lg">Timezone Scheduler</h1>
    </div>
  </header>

  <main class="mx-auto grid max-w-3xl gap-10 px-4 py-6 sm:px-6 sm:py-8">
    <section aria-label="World Clock">
      <div class="flex items-start justify-between gap-4">
        <button type="button" data-action="click->world-clock#openAdd" class="rounded-xl bg-blue-700 px-4 py-3 font-bold text-white shadow-sm transition hover:bg-blue-800 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">+ Add City</button>
        <span class="shrink-0 text-sm text-slate-500" data-world-clock-target="cityCount">0 of 10 cities</span>
      </div>

      <p class="min-h-6 pt-2 text-sm text-amber-700" data-world-clock-target="message" role="status"></p>
      <div class="grid min-h-32 items-center justify-items-stretch border-y border-dashed border-slate-300 py-2" data-world-clock-target="cityList" aria-live="polite">
        <p class="text-center text-slate-500">Choose your city to start comparing local time.</p>
      </div>

      <div data-world-clock-target="searchPanel" role="dialog" aria-modal="true" aria-labelledby="city-search-heading" hidden>
        <div class="fixed inset-0 z-50 flex items-center justify-center p-4">
          <div class="absolute inset-0 bg-slate-950/50" data-action="click->world-clock#closeSearch"></div>
          <div class="relative z-10 h-[28rem] w-[calc(100%-2rem)] max-w-lg overflow-y-auto rounded-2xl bg-white p-5 shadow-2xl sm:p-6">
            <div class="flex items-center justify-between gap-4">
              <h3 id="city-search-heading" class="text-lg font-bold">Search cities</h3>
              <button type="button" class="rounded-lg px-2 py-1 text-sm font-bold text-blue-700 hover:bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-500" data-action="click->world-clock#closeSearch">Close</button>
            </div>
            <label for="city-search-input" class="mt-5 block text-sm font-bold">City or country</label>
            <input id="city-search-input" type="search" autocomplete="off" placeholder="Tokyo or Canada" data-world-clock-target="searchInput" data-action="input->world-clock#search" class="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3 outline-none focus:border-blue-600 focus:ring-2 focus:ring-blue-200">
            <div class="mt-3 grid gap-2" data-world-clock-target="searchResults" aria-live="polite"></div>
          </div>
        </div>
      </div>
    </section>
  </main>
</div>
